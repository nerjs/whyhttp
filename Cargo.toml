[package]
name = "whyhttp"
version = "0.1.0"
edition = "2024"

[dependencies]
tiny_http = "0.12.0"

[dev-dependencies]
rstest = "0.26.1"
tokio = { version = "1.50.0", features = ["full"] }
reqwest = { version = "0.13.2", features = ["blocking"] }
